-- add the intro animation
-- get the terminal working
-- figure out how to load apps
-- figure out how to use json files to save data
-- make a settings app (probably just some commands in the terminal... oh yeah, also make the terminal lol)
-- add sound effects (like a startup sound, beeps and boops for the terminal, crash sounds, etc.)
-- add a crash screen (with a blue screen of death and a sad trombone sound effect)
-- add a shutdown animation (maybe just the logo fading out?)